FROM node:20-alpine

WORKDIR /app

COPY radio/package.json ./
RUN npm install --production

COPY radio/ ./

EXPOSE 3200

HEALTHCHECK --interval=30s --timeout=5s --start-period=10s \
  CMD wget -qO- http://localhost:3200/health || exit 1

CMD ["node", "server.js"]
